# Copy this file to .env and fill in real values once a backend exists.
# Vite only exposes variables prefixed with VITE_ to client-side code.

# Base URL for the backend API (used by src/services/apiClient.js).
# Leave empty while running on mock data.
VITE_API_BASE_URL=
